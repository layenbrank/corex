# shell.run — 最小示例
# 运行: corex run examples/actions/shell.run.yaml
# 交互式提问的自动应答（input）见 ../directives/shell-input-demo.yaml
name: action-shell-run
description: "Action: shell.run"
permissions:
  shell: true
steps:
  - id: demo
    action: shell.run
    params:
      command: echo
      args: ["hello from shell.run"]
      host: none
      wait: sync
    save_to: result
